diff --git a/Mac/InputInjector.swift b/Mac/InputInjector.swift
index 60b92f0..0b6d2a1 100644
--- a/Mac/InputInjector.swift
+++ b/Mac/InputInjector.swift
@@ -110,6 +110,39 @@ final class InputInjector {
         event.post(tap: .cghidEventTap)
     }
 
+    func handleText(_ text: String) {
+        guard !text.isEmpty,
+              let down = CGEvent(keyboardEventSource: source, virtualKey: 0, keyDown: true),
+              let up = CGEvent(keyboardEventSource: source, virtualKey: 0, keyDown: false) else { return }
+        let units = Array(text.utf16)
+        units.withUnsafeBufferPointer { buf in
+            down.keyboardSetUnicodeString(stringLength: units.count, unicodeString: buf.baseAddress!)
+            up.keyboardSetUnicodeString(stringLength: units.count, unicodeString: buf.baseAddress!)
+        }
+        down.post(tap: .cghidEventTap)
+        up.post(tap: .cghidEventTap)
+    }
+
+    func handleKey(_ key: String) {
+        let code: CGKeyCode
+        switch key {
+        case "backspace": code = 51
+        case "enter": code = 36
+        case "tab": code = 48
+        case "escape": code = 53
+        case "left": code = 123
+        case "right": code = 124
+        case "down": code = 125
+        case "up": code = 126
+        default: return
+        }
+        CGEvent(keyboardEventSource: source, virtualKey: code, keyDown: true)?.post(tap: .cghidEventTap)
+        CGEvent(keyboardEventSource: source, virtualKey: code, keyDown: false)?.post(tap: .cghidEventTap)
+    }
+
     /// dx/dy in display pixels, natural-scrolling sign from the phone.
     /// Scroll events take points, so convert via the display's pixel scale.
     func handleScroll(dx: Double, dy: Double) {
diff --git a/Mac/MacSender.swift b/Mac/MacSender.swift
index 582e85f..d8c4ef1 100644
--- a/Mac/MacSender.swift
+++ b/Mac/MacSender.swift
@@ -1775,6 +1775,29 @@ final class MacSender: NSObject, SCStreamOutput, SCStreamDelegate {
             if let dx = obj["dx"] as? Double, let dy = obj["dy"] as? Double {
                 inputInjector?.handleScroll(dx: dx, dy: dy)
             }
+        case "text":
+            if let text = obj["text"] as? String { inputInjector?.handleText(text) }
+        case "key":
+            if let key = obj["key"] as? String { inputInjector?.handleKey(key) }
+        case "resolution":
+            if mode == .extend,
+               let width = obj["width"] as? Int,
+               let height = obj["height"] as? Int,
+               width >= 640, height >= 480,
+               var info = lastHello {
+                // PhoneInfo dimensions are physical pixels. OpenDisplay's virtual
+                // desktop is Retina 2x, so a requested macOS logical resolution is
+                // represented as twice that size before using the existing resize path.
+                info.pixelsWide = width * 2
+                info.pixelsHigh = height * 2
+                lastHello = info
+                Task {
+                    try? await Task.sleep(for: .milliseconds(80))
+                    guard let current = self.lastHello,
+                          current.pixelsWide == width * 2,
+                          current.pixelsHigh == height * 2 else { return }
+                    await self.reconfigure(current)
+                }
+            }
         case "pencil":
             if let phase = obj["phase"] as? String,
                let x = obj["x"] as? Double,
